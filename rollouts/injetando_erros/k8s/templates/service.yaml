apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name }}
  name: {{ .Release.Name }}
spec:
  ports:
  - port: {{ .Values.injetandoErros.port }}
    protocol: TCP
    targetPort: {{ .Values.injetandoErros.port }}
  selector:
    app: {{ .Release.Name }}
  type: ClusterIP